[project]
name = "1mm-ai"
version = "1.0.0"
description = "Precision Performance Intelligence"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.104.0",
    "mistralai>=1.0.0",
    "python-multipart>=0.0.32",
    "uvicorn>=0.24.0",
]

[tool.vercel]
entrypoint = "app:app"
